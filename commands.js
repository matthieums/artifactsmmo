import { moveTo, fight, gather, rest, craft, deposit, withdraw, equip, exit } from "./actions.js";

export const commands = {
  move: moveTo,
  fight: fight,
  rest: rest,
  gather: gather,
  craft: craft,
  deposit: deposit,
  withdraw: withdraw,
  equip: equip,
  exit: exit
}